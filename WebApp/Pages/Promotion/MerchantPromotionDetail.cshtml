@page
@model WebApp.Pages.Promotion.MerchantPromotionDetailModel
@{
    ViewData["Title"] = Model.PromotionFound ? Model.PromotionInfo.MerchantPromotionName : "Promoción no encontrada";
}

<!-- Estilos específicos para detalle de promoción -->
<style>
    .promotion-hero {
        background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
        color: white;
        padding: 3rem 0 2rem;
        margin-bottom: 2rem;
    }
    
    .promotion-image {
        width: 100px;
        height: 100px;
        border-radius: 12px;
        object-fit: cover;
        border: 3px solid white;
        box-shadow: 0 2px 8px rgba(0,0,0,0.15);
    }
    
    .promotion-info-card {
        border: none;
        border-radius: 12px;
        box-shadow: 0 2px 12px rgba(0,0,0,0.1);
        overflow: hidden;
    }
    
    .discount-badge {
        font-size: 2rem;
        font-weight: bold;
        background: linear-gradient(45deg, #28a745, #20c997);
        color: white;
        border-radius: 50%;
        width: 80px;
        height: 80px;
        display: flex;
        align-items: center;
        justify-content: center;
        box-shadow: 0 2px 8px rgba(0,0,0,0.2);
    }
    
    .merchant-card {
        background: #f8f9fa;
        border: 1px solid #e9ecef;
        border-radius: 12px;
        padding: 1.5rem;
    }
    
    .back-button {
        position: sticky;
        top: 20px;
        z-index: 100;
    }
    
    @@media (max-width: 768px) {
        .promotion-hero {
            padding: 2rem 0 1rem;
        }
        
        .promotion-image {
            width: 70px;
            height: 70px;
        }
        
        .discount-badge {
            width: 60px;
            height: 60px;
            font-size: 1.5rem;
        }
    }
</style>

@if (!Model.PromotionFound)
{
    <!-- Promotion Not Found -->
    <div class="container py-5">
        <div class="row justify-content-center">
            <div class="col-md-6 text-center">
                <i class="bi bi-gift text-muted" style="font-size: 5rem;"></i>
                <h2 class="mt-4 text-muted">Promoción no encontrada</h2>
                <p class="text-muted mb-4">@Model.Message</p>
                <a href="/" class="btn btn-primary">
                    <i class="bi bi-arrow-left me-2"></i>Volver al inicio
                </a>
            </div>
        </div>
    </div>
}
else
{
    <!-- Back Button -->
    <div class="container-fluid">
        <div class="back-button mt-3">
            <a href="javascript:history.back()" class="btn btn-outline-light">
                <i class="bi bi-arrow-left me-2"></i>Volver
            </a>
        </div>
    </div>

    <!-- Promotion Hero Section -->
    <section class="promotion-hero">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-md-2 text-center">
                    @if (!string.IsNullOrEmpty(Model.PromotionInfo.MerchantPromotionImage))
                    {
                        <img src="@Model.PromotionInfo.MerchantPromotionImage" 
                             alt="@Model.PromotionInfo.MerchantPromotionName" 
                             class="promotion-image">
                    }
                    else
                    {
                        <div class="promotion-image d-flex align-items-center justify-content-center bg-white text-success">
                            <i class="@Model.GetPromotionTypeIcon()" style="font-size: 2rem;"></i>
                        </div>
                    }
                </div>
                <div class="col-md-8">
                    <h1 class="mb-3">@Model.PromotionInfo.MerchantPromotionName</h1>
                    <div class="mb-3">
                        <span class="badge @Model.GetPromotionStatusBadgeClass() me-2">
                            @Model.GetPromotionStatusText()
                        </span>
                        <span class="badge bg-light text-dark">@Model.PromotionInfo.PromotionType</span>
                    </div>
                    <p class="lead mb-3">@Model.PromotionInfo.MerchantPromotionDescription</p>
                    
                    @if (Model.IsPromotionActive())
                    {
                        <div class="alert alert-light border border-white">
                            <i class="bi bi-check-circle me-2"></i>
                            <strong>¡Promoción vigente!</strong> 
                            @if (Model.GetDaysRemaining() > 0)
                            {
                                <span>Quedan @Model.GetDaysRemaining() días para aprovecharla.</span>
                            }
                        </div>
                    }
                </div>
            </div>
        </div>
    </section>

    <!-- Promotion Details -->
    <section class="py-4">
        <div class="container">
            <div class="row">
                <div class="col-md-8">
                    <!-- Información de la Promoción -->
                    <div class="promotion-info-card card mb-4">
                        <div class="card-header bg-success text-white">
                            <h4 class="mb-0">
                                <i class="bi bi-info-circle me-2"></i>Detalles de la Promoción
                            </h4>
                        </div>
                        <div class="card-body">
                            <div class="row">
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <h6 class="text-muted mb-1">Tipo de Promoción</h6>
                                        <p class="mb-0">
                                            <i class="@Model.GetPromotionTypeIcon() me-2 text-success"></i>
                                            @Model.PromotionInfo.PromotionType
                                        </p>
                                    </div>
                                    <div class="mb-3">
                                        <h6 class="text-muted mb-1">Descuento</h6>
                                        <p class="mb-0 text-success fw-bold fs-4">
                                            @Model.PromotionInfo.DiscountPercentage.ToString("0.0")%
                                        </p>
                                    </div>
                                    <div class="mb-3">
                                        <h6 class="text-muted mb-1">Reembolso Máximo</h6>
                                        <p class="mb-0">
                                            ₡@Model.PromotionInfo.MaxRefund.ToString("N0")
                                        </p>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="mb-3">
                                        <h6 class="text-muted mb-1">Disponibles</h6>
                                        <p class="mb-0">
                                            <i class="bi bi-box me-2"></i>
                                            @Model.PromotionInfo.AvailableQuantity unidades
                                        </p>
                                    </div>
                                    <div class="mb-3">
                                        <h6 class="text-muted mb-1">Fecha de Inicio</h6>
                                        <p class="mb-0">
                                            <i class="bi bi-calendar-check me-2"></i>
                                            @Model.PromotionInfo.StartDate.ToString("dd/MM/yyyy")
                                        </p>
                                    </div>
                                    <div class="mb-3">
                                        <h6 class="text-muted mb-1">Fecha de Vencimiento</h6>
                                        <p class="mb-0">
                                            <i class="bi bi-calendar-x me-2"></i>
                                            @Model.PromotionInfo.EndDate.ToString("dd/MM/yyyy")
                                        </p>
                                    </div>
                                </div>
                            </div>
                            
                            @if (!string.IsNullOrEmpty(Model.PromotionInfo.MerchantPromotionTerms))
                            {
                                <hr>
                                <h6 class="text-muted mb-2">Términos y Condiciones</h6>
                                <div class="alert alert-light border-start border-4 border-success">
                                    <small>@Model.PromotionInfo.MerchantPromotionTerms</small>
                                </div>
                            }
                        </div>
                    </div>
                </div>
                
                <div class="col-md-4">
                    <!-- Información del Comercio -->
                    <div class="merchant-card mb-4">
                        <h5 class="mb-3">
                            <i class="bi bi-shop text-success me-2"></i>Comercio Participante
                        </h5>
                        <div class="text-center mb-3">
                            @if (!string.IsNullOrEmpty(Model.MerchantInfo.LogoImage))
                            {
                                <img src="@Model.MerchantInfo.LogoImage" alt="@Model.MerchantInfo.MerchantName" 
                                     class="rounded" style="width: 110px; height: 60px; object-fit: cover;">
                            }
                            else
                            {
                                <div class="bg-success text-white rounded d-flex align-items-center justify-content-center"
                                     style="width: 110px; height: 60px; margin: 0 auto;">
                                    <i class="bi bi-shop" style="font-size: 1.5rem;"></i>
                                </div>
                            }
                        </div>
                        <h6 class="text-center">@Model.MerchantInfo.MerchantName</h6>
                        
                        <div class="mt-3">
                            @if (!string.IsNullOrEmpty(Model.MerchantInfo.Email))
                            {
                                <p class="small mb-1">
                                    <i class="bi bi-envelope me-2"></i>@Model.MerchantInfo.Email
                                </p>
                            }
                            @if (!string.IsNullOrEmpty(Model.MerchantInfo.ContactPhone))
                            {
                                <p class="small mb-1">
                                    <i class="bi bi-telephone me-2"></i>@Model.MerchantInfo.ContactPhone
                                </p>
                            }
                        </div>
                        
                        <div class="d-grid mt-3">
                            <a href="/Merchant/MerchantDetail?merchantId=@Model.MerchantInfo.ID" class="btn btn-success">
                                <i class="bi bi-eye me-2"></i>Ver Comercio
                            </a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- CTA Section -->
    <section class="py-5 bg-light">
        <div class="container text-center">
            @if (Model.IsPromotionActive())
            {
                <h3 class="mb-3 text-success">¡Aprovecha esta promoción ahora!</h3>
                <p class="lead mb-4">Descarga Yavi y comienza a ahorrar con @Model.MerchantInfo.MerchantName</p>
                <div class="d-flex justify-content-center gap-3 flex-wrap">
                    <a href="/Register/UserRegister" class="btn btn-success btn-lg">
                        <i class="bi bi-download me-2"></i>Regístrate
                    </a>
                    <a href="/Merchant/MerchantDetail?merchantId=@Model.MerchantInfo.ID" class="btn btn-outline-success btn-lg">
                        <i class="bi bi-shop me-2"></i>Ver Comercio
                    </a>
                </div>
            }
            else
            {
                <h3 class="mb-3 text-muted">Esta promoción no está disponible</h3>
                <p class="lead mb-4">Explora otras promociones disponibles en Yavi</p>
                <a href="/" class="btn btn-primary btn-lg">
                    <i class="bi bi-gift me-2"></i>Ver Otras Promociones
                </a>
            }
        </div>
    </section>
}

@section Scripts {
    <script>
        // Smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });
    </script>
}